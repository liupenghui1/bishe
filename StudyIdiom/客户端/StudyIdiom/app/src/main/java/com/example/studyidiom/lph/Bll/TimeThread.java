package com.example.studyidiom.lph.Bll;

import android.content.Context;
import android.widget.Chronometer;

public class TimeThread extends Chronometer{
    private Context con;
	public TimeThread(Context context) {
		super(context);
		// TODO 自动生成的构造函数存根
		this.con=context;
	}

}
